{
	"scripts": {
		"lint": "pnpm exec dprint check && pnpm exec tsc --noEmit --project tsconfig.app.json && pnpm exec tsc --noEmit --project tsconfig.node.json && pnpm exec eslint . --max-warnings=0"
	},
	"devDependencies": {
		"@eslint/js": "^9",
		"globals": "^16",
		"dprint": "^0.50.2",
		"eslint": "^9",
		"eslint-config-preact": "^2",
		"husky": "^9",
		"typescript-eslint": "^8"
	}
}